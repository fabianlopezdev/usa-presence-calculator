#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Run linting
echo "🔍 Running linters..."
pnpm lint

# Run TypeScript type checking
echo "📝 Running TypeScript checks..."
pnpm typecheck

# Run tests
echo "🧪 Running tests..."
pnpm test
